services:
  app:
    container_name: app
    build:
      context: ../
      dockerfile: ./docker/Dockerfile.development
    volumes:
      - type: bind
        source: ../
        target: /workspaces/strategic-programming-exercise/
    tty: true
